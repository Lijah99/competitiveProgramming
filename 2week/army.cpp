#include <iostream>
#include <algorithm>    
#include <vector>       

using namespace std;

int main()
{
	int tests; cin >> tests;
	int numG, numM;
	vector<int> God, Mech;
	int temp;
	for(int i = 0; i < tests; i++)
	{
		God.empty(); Mech.empty();
		sG = 0; sM = 0;
		cin >> numG >> numM;
		
		
	}
	return 0;
}